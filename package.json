{
  "name": "cf-image-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker AI Image Generator with OpenAI-compatible API",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret": "wrangler secret put API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
